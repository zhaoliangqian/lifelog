http
属于应用层协议，在tcp协议之上，也可以在ssl/tsl之上，也就是https。http是无状态的客户端服务端协议

特点
1、无状态
2、协议简单，通信速度快
3、灵活，通过content-type设置，可以传输任何类型的数据
4、http1.1之前是非持久型传输，在1.1之后，多个web资源的请求可以共用一个tcp连接，在http2.0之后，tcp连接可以多路复用，实现并发进行web资源的传输

过程
1、client发起请求建立连接
  内容包含4个部分：
    1。请求行 url 协议版本 
    2、请求头 host 客户端信息等
    3、空行 必填
    4、请求体，可以为空
2、server响应数据
  内容包含部分：
    1、状态行 状态 协议版本
    2、消息头 文件类型 客户端信息 消息长度等
    3、空行 必填
    4、具体消息信息
    
状态码：
1** 消息已经接受，继续处理
2** 消息处理完成
3** 重定向
4** 客户端错误
5** 服务端错误
解释下301 302的区别： 301永久性转移，302为暂时性转移，主要对存储引擎有影响，302的请求存储引擎只会处理内容和新地址的关系

请求的方法 
get post最常用
header类似于get，响应只有header没有消息体
put像server存储文件
option查看资源支持的操作类型
trace 用于返回服务端收到的请求信息

http2的特性：
1、消息头压缩
2、tcp多路复用
http学习
https://www.jianshu.com/p/67c541a421f9

https执行过程
1、client ---（https）---> server 请求时提供客户端支持的加密算法等信息
2、server---(证书)------>client server确认加密、hash算法
3、client ----(公钥加密的随机数R)---> server client校验证书，成功后，随机生成r，然后从证书中的公钥对r加密
4、server-----(响应体)--->client server用私钥将R解密，然后使用R对响应体加密，发送客户端
